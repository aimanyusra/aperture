<div class="row">	
	<div class="text-center">
		<p><%= @greet.sample %> by <strong><%= @photo.user.name %></strong></p>
		<%= image_tag @photo.photo_images[0].image.photoset %>
		<strong><p>Tags: </p></strong> 		 
		<% @photo.tags.each do |x| %>
		  <%= x.name %>
		<% end %>
	</div>
</div>

<div class="row">
	<div class="text-center">
		<br>
		<p><%= link_to 'Submit Another?', submit_path %></p>
	</dir>
</div>

<% if current_user.id == @photo.user.id %>
	<%= link_to 'Edit', edit_photo_path(@photo) %> |
<% end %>
<%= link_to 'Back', photos_path %>